# Shared ML + backend deps. Python 3.11 recommended for Lambda parity; 3.13 supported below.

numpy>=2.0
pandas>=2.1
scikit-learn>=1.5.2
boto3>=1.34
pytest>=8.0

# If pickle load fails after sklearn upgrade, retrain:
#   python ml/scripts/train.py
