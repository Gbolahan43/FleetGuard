AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: FleetGuard Path A — real-time scoring (Lambda + API Gateway + DynamoDB)

# Hackathon / APN tags — applied per resource below.
# Standard (all resources): aws-apn-id pc:8l8gcn23lmlgammd8572tk6va, event oneWithAI, team E-Evo
# Gen AI (Bedrock Lambda):  aws-apn-id pc:a8xnp70u5w0s41039u52e6iuj, event oneWithAI, team E-Evo

Parameters:
  BedrockModelId:
    Type: String
    Default: us.anthropic.claude-opus-4-6-v1
    Description: Bedrock inference profile for Claude Opus 4.6 incident reports

Globals:
  Function:
    Timeout: 30
    MemorySize: 512

Resources:
  ModelBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      Tags:
        - Key: aws-apn-id
          Value: pc:8l8gcn23lmlgammd8572tk6va
        - Key: event
          Value: oneWithAI
        - Key: team
          Value: E-Evo

  IncidentsTable:
    Type: AWS::DynamoDB::Table
    Properties:
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: incident_id
          AttributeType: S
        - AttributeName: vehicle_id
          AttributeType: S
      KeySchema:
        - AttributeName: incident_id
          KeyType: HASH
      GlobalSecondaryIndexes:
        - IndexName: vehicle_id-index
          KeySchema:
            - AttributeName: vehicle_id
              KeyType: HASH
          Projection:
            ProjectionType: ALL
      Tags:
        - Key: aws-apn-id
          Value: pc:8l8gcn23lmlgammd8572tk6va
        - Key: event
          Value: oneWithAI
        - Key: team
          Value: E-Evo

  VehicleStateTable:
    Type: AWS::DynamoDB::Table
    Properties:
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: vehicle_id
          AttributeType: S
      KeySchema:
        - AttributeName: vehicle_id
          KeyType: HASH
      Tags:
        - Key: aws-apn-id
          Value: pc:8l8gcn23lmlgammd8572tk6va
        - Key: event
          Value: oneWithAI
        - Key: team
          Value: E-Evo

  HttpApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName: $default
      CorsConfiguration:
        AllowOrigins:
          - "*"
        AllowMethods:
          - GET
          - POST
          - OPTIONS
        AllowHeaders:
          - "*"
      Tags:
        aws-apn-id: pc:8l8gcn23lmlgammd8572tk6va
        event: oneWithAI
        team: E-Evo

  ScoreFunction:
    Type: AWS::Serverless::Function
    Properties:
      PackageType: Image
      Architectures:
        - x86_64
      # Gen AI service — invokes Amazon Bedrock for incident reports
      Tags:
        aws-apn-id: pc:a8xnp70u5w0s41039u52e6iuj
        event: oneWithAI
        team: E-Evo
      Environment:
        Variables:
          MODEL_BUCKET: !Ref ModelBucket
          MODEL_PREFIX: fleetguard-model
          DYNAMO_TABLE: !Ref IncidentsTable
          STATE_TABLE: !Ref VehicleStateTable
          BEDROCK_REGION: !Ref AWS::Region
          BEDROCK_MODEL_ID: !Ref BedrockModelId
      Policies:
        - S3ReadPolicy:
            BucketName: !Ref ModelBucket
        - DynamoDBCrudPolicy:
            TableName: !Ref IncidentsTable
        - DynamoDBCrudPolicy:
            TableName: !Ref VehicleStateTable
        - Statement:
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
              Resource: "*"
      Events:
        ScoreRoute:
          Type: HttpApi
          Properties:
            ApiId: !Ref HttpApi
            Path: /score
            Method: POST
        IncidentsRoute:
          Type: HttpApi
          Properties:
            ApiId: !Ref HttpApi
            Path: /incidents
            Method: GET
    Metadata:
      Dockerfile: Dockerfile.lambda
      DockerContext: ../../ml

  WarmRule:
    Type: AWS::Events::Rule
    Properties:
      Description: Keep Lambda warm during demo windows
      ScheduleExpression: rate(5 minutes)
      State: ENABLED
      Tags:
        - Key: aws-apn-id
          Value: pc:8l8gcn23lmlgammd8572tk6va
        - Key: event
          Value: oneWithAI
        - Key: team
          Value: E-Evo
      Targets:
        - Id: ScoreFunctionWarmTarget
          Arn: !GetAtt ScoreFunction.Arn
          Input: '{"warmup": true}'

  WarmRulePermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref ScoreFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt WarmRule.Arn

Outputs:
  ApiUrl:
    Description: API Gateway HTTP API base URL (NEXT_PUBLIC_API_URL)
    Value: !Sub "https://${HttpApi}.execute-api.${AWS::Region}.amazonaws.com"
  ModelBucketName:
    Description: S3 bucket for model artifacts — run upload_models script
    Value: !Ref ModelBucket
  IncidentsTableName:
    Value: !Ref IncidentsTable
  VehicleStateTableName:
    Value: !Ref VehicleStateTable
  ScoreFunctionArn:
    Value: !GetAtt ScoreFunction.Arn
